syntax = "proto3";
package message;

enum MessageType {
  REGISTER = 0;
  ENTER_WORLD = 1;
  LEAVE_WORLD = 2;

  GRID = 3;
  COMMAND = 4;
}

message Message {
  MessageType type = 1;

  bytes content = 2;
}

message RegisterName {
  string name = 1;
}